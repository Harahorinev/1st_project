// import classes from './Settings.module.css'
import classes from './Settings.module.css'
import React from 'react'

const Settings = (props) => {
  return <div className={classes.item}>Settings</div>
}

export default Settings
